# LLM Interaction Log

---

## Metadata

```yaml
group_id: "G04"
repository_url: "https://github.com/..."
students:
  - matricola: "66469"
    name: "Matteo Parasuco"
  - matricola: "66474"
    name: "Igor Derosas"
last_update: "2026-06-03"
log_id: "G04-01"
```

---

# Interaction Entries

---

## Interaction G04-06-03-001

### 1. LLM and chat information

```yaml
llm_name: "Gemini"
llm_version_or_model: "Gemini 3.1 Pro (Preview)"
chat_name_or_identifier: "Discuss specification details"
interaction_mode: "IDE_assistant"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"
```

### 3. Project phase

```yaml
project_phase:
  - "requirements_understanding"
  - "documentation"
  - "repository_structure"
```

### 4. Input files and/or context provided to the LLM

```yaml
input_context:
  files_uploaded:
    - file_name: "specifiche.txt"
      repository_path: ".artifacts/specifiche.txt"
      description: "Project requirements and specs"
    - file_name: "group_info.yaml"
      repository_path: "group_info.yaml"
      description: "Group member info"
    - file_name: "llm_interaction_log_template.md"
      repository_path: "reports/llm_usage/llm_interaction_log_template.md"
      description: "Template for LLM logging"
  code_snippets: "no"
  error_messages: "no"
  dataset_excerpts: "no"
  prompt_generated_by: null 
```

### 5. Student prompt

```text
Hi! I have reviewed the software engineering project specifications for the Python binary classification application. I would like to design the architecture focusing on two main areas:

1. Machine Learning & QUBO Implementation: 
- According to the constraints, we need to implement a pipeline consisting of data preprocessing (z-score normalization, removing columns with nulls > threshold based on a custom `minPercValid`), QUBO-based feature selection, and a binary classifier (including Random Forest and two other algorithms). 
- How should we mathematically formulate the QUBO cost function matrix (balancing Spearman correlation for influence vs independence using alpha) and iteratively tune alpha within the required `alpha_computations` limit to hit the target `percSelected` feature count? Which open-source QUBO solvers (e.g., D-Wave dimod, simulated annealing) would fit our standard Python CLI/GUI requirement best?

2. Report and Log Management:
- The specification explicitly requires a rigid artifact structure under `reports/llm_usage/` consisting of progressive Markdown logs (LOG-GXX-NN.md) detailing our AI interactions, along with the saved code snippets in `reports/llm_usage/files/`.
- Let's discuss an effective modular design pattern in Python to handle run-time output generation (CSV predictions with row_n, target, prediction, score; and JSON runtime statistics for preprocessing, feature selection, and classification metrics).
```

### 6. LLM response

```text
The LLM provided a detailed breakdown of the mathematical implementation of the QUBO problem (calculating Spearman correlation to populate the dense Q matrix, building a binary search loop for alpha, and using simulated annealing via 'neal'). It also explained how to structure the scikit-learn models focusing on maximizing F1-score for minority classes (`class_weight='balanced'`) to get the bonus points.
Finally, the LLM proposed using a VS Code Copilot custom Prompt (`log.prompt.md`) to automatically generate this exact documentation file at the end of each session, providing the setup and configuration of the prompt file itself in `.github/prompts/log.prompt.md`.
```

### 7. Files generated or downloaded from the LLM response

```yaml
generated_or_downloaded_files:
  - unique_file_name: "log.prompt.md"
    repository_path: ".github/prompts/log.prompt.md"
    file_type: "md"
    created_from_response: true
    short_description: "Automated custom prompt to generate LLM interaction logs based on the university template."
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true
  modified_before_use: false
  description_of_modifications: null
  related_repository_files:
    - ".github/prompts/log.prompt.md"
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: "no"
```

### 10. Usefulness and reliability assessment

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: true
  notes: "The LLM accurately parsed the complex project requirements and successfully set up a robust logging automation workflow in VS Code."

---

## Interaction G04-06-03-002

### 1. LLM and chat information

```yaml
llm_name: "Gemini (Antigravity)"
llm_version_or_model: "Gemini 3.5 Flash"
chat_name_or_identifier: "Implement Preprocessing Module"
interaction_mode: "IDE_assistant"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"
```

### 3. Project phase

```yaml
project_phase:
  - "preprocessing"
  - "testing"
```

### 4. Input files and/or context provided to the LLM

```yaml
input_context:
  files_uploaded:
    - file_name: "prompt_preprocessing.md"
      repository_path: ".artifacts/prompt_preprocessing.md"
      description: "Detailed prompt for implementing the preprocessing phase"
  code_snippets: "yes"
  error_messages: "no"
  dataset_excerpts: "yes"
  prompt_generated_by: null 
```

### 5. Student prompt

```text
follow the prompt in .artifacts/prompt_preprocessing.md
```

### 6. LLM response

```text
The LLM generated a complete, self-contained implementation of `src/qubo_project/preprocessing.py`, implementing both the API function `fit_normalize(...)` and a CLI interface using argparse. It also provided a set of unit tests in `tests/test_preprocessing.py` using pytest.
```

### 7. Files generated or downloaded from the LLM response

```yaml
generated_or_downloaded_files:
  - unique_file_name: "preprocessing.py"
    repository_path: "src/qubo_project/preprocessing.py"
    file_type: "py"
    created_from_response: true
    short_description: "Preprocessing module with fit_normalize and CLI."
    referenced_in_response: true
  - unique_file_name: "test_preprocessing.py"
    repository_path: "tests/test_preprocessing.py"
    file_type: "py"
    created_from_response: true
    short_description: "Unit tests for the preprocessing module."
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true
  modified_before_use: false
  description_of_modifications: null
  related_repository_files:
    - "src/qubo_project/preprocessing.py"
    - "tests/test_preprocessing.py"
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: "yes"
  issue_categories:
    - "syntax_error"
  description: "During initial manual CLI execution, pandas raised a PerformanceWarning regarding DataFrame fragmentation due to iteratively inserting columns into normalized_df."
  how_issue_was_resolved: "Refactored column combination to construct a dictionary of normalized columns first, then initialized the final DataFrame in a single step."
```

### 10. Usefulness and reliability assessment

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: true
  notes: "The implementation is robust, performs z-score standard scaling perfectly, handles constant feature edge cases safely, and all tests pass."
```

```
