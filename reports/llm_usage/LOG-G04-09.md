# LLM Interaction Log

---

## Metadata

```yaml
group_id: "G04"
repository_url: "https://github.com/SolidusRam/isw-qubo-classification-G04"
students:
  - matricola: "66469"
    name: "Matteo Parasuco"
  - matricola: "66474"
    name: "Igor Derosas"
last_update: "2026-06-08"
log_id: "G04-09"
```

---

# Interaction Entries

---

## Interaction G04-06-08-002

### 1. LLM and chat information

```yaml
llm_name: "Gemini"
llm_version_or_model: "Gemini 3.1 Pro (High)"
chat_name_or_identifier: "Feature selection QUBO optimization and bug fixing"
interaction_mode: "IDE_assistant"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"
```

### 3. Project phase

```yaml
project_phase:
  - "feature_selection_qubo"
  - "bug_fixing"
```

### 4. Input files and/or context provided to the LLM

```yaml
input_context:
  files_uploaded: []
  code_snippets: "no"
  error_messages: "no"
  dataset_excerpts: "no"
  prompt_generated_by: "user" 
```

### 5. Student prompt

```text
1. I would like you to fix the critical issues found during the audit: feature selection not converging, wrong report filename, and empty cost column in the CSV.
2. The execution was quite slow, can you test the latest changes again and improve the performance?
3. Is this a significant improvement over the previous QUBO function? Was it wrong before?
4. Are we going against the PDF specifications by using a greedy post-processing?
```

### 6. LLM response

```text
The LLM fixed the critical issues identified in the audit. It renamed the project report file to match the exact required filename and fixed an issue where the `cost` column in the optimizations CSV was empty due to `NaN` values in the Spearman correlation matrix (caused by constant features). 

To fix the non-convergence of the feature selection, the LLM first attempted to drastically increase the `num_reads` and `num_sweeps` of the Simulated Annealing solver. However, this proved to be extremely slow. The LLM then introduced a hybrid approach: using the SA solver to find a good global region, followed by a greedy post-processing adjustment that adds or removes features based on their marginal QUBO cost contribution until exactly `K` features are selected. 

The initial implementation of the greedy adjustment contained an unused O(N^3) logic flaw that made it very slow (~2.5 minutes for 20 iterations). The LLM identified the bottleneck and optimized it to O(N), bringing the execution time for 100 iterations down to just ~12 seconds.

Finally, the LLM clarified that the original "pure" QUBO formulation was mathematically correct but engineerically flawed due to the difficulty of enforcing a hard cardinality constraint on a dense graph using only the `alpha` penalty. It reassured the user that the hybrid approach strictly follows the PDF specifications while ensuring the `K` features constraint is met perfectly.
```

### 7. Files generated or downloaded from the LLM response

```yaml
generated_or_downloaded_files: []
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true
  modified_before_use: false
  description_of_modifications: null
  related_repository_files:
    - "src/qubo_project/feature_selection.py"
    - "reports/project_report.yaml"
    - "group_info.yaml"
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: "yes"
  description: "The first implementation of the greedy post-processing algorithm contained an unused O(N^3) calculation that severely impacted execution speed. The LLM successfully identified and resolved this issue in a subsequent prompt."
```

### 10. Usefulness and reliability assessment

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: true
  notes: "The LLM successfully fixed the critical convergence issues and significantly optimized the performance of the feature selection script. It also provided a solid theoretical explanation for the hybrid QUBO approach."
```
