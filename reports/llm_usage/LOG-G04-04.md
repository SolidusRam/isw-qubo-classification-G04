# LLM Interaction Log

---

## Metadata

```yaml
group_id: "G04"
repository_url: "https://github.com/isw-qubo-classification-G04"
students:
  - matricola: "66469"
    name: "Matteo Parasuco"
  - matricola: "66474"
    name: "Igor Derosas"
last_update: "2026-06-05"
log_id: "G04-04"
```

---

# Interaction Entries

---

## Interaction G04-06-05-001

### 1. LLM and chat information

```yaml
llm_name: "Gemini"
llm_version_or_model: "Gemini 3.1 Pro"
chat_name_or_identifier: "IDE_session_phase2_implementation"
interaction_mode: "IDE_assistant"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"
```

### 3. Project phase

```yaml
project_phase:
  - "feature_selection_qubo"
```

### 4. Input files and/or context provided to the LLM

```yaml
input_context:
  files_uploaded:
    - file_name: "README.md"
      repository_path: ".artifacts/README.md"
      description: "Project specification with mathematical appendix"
  code_snippets: "no"
  error_messages: "no"
  dataset_excerpts: "no"
  prompt_generated_by: null 
```

### 5. Student prompt

```text
Write the python code for `src/qubo_project/feature_selection.py` according to Phase 2 of the project described in the README.
The function `select_features` must have the exact signature required.
It should read the dataset, calculate the Spearman correlation matrix (absolute values), and build a QUBO model to select `percSelected` features (plus or minus `allowance`).

The QUBO minimizes independence minus influence, which translates mathematically to having `-alpha * |rho_Vj|` on the diagonal and `2 * (1 - alpha) * |rho_jk|` outside the diagonal for the `x_j x_k` terms in dimod format. 
We should run a binary search to find the correct `alpha` between 0 and 1, testing up to `alpha_computations` times. Use `neal.SimulatedAnnealingSampler()` with `num_reads=100` and the provided seed for reproducibility.

Save the optimization results in the csv file, split the dataset without shuffling to keep the first M items for training and the rest for testing according to `percTest`. Save the reduced datasets and the JSON stats exactly in the format required by the assignment guidelines.
```

### 6. LLM response

```text
I have created the `feature_selection.py` module conforming to your request. The script calculates the Spearman correlation matrix using pandas, defines the objective QUBO correctly based on the instructions, and implements an efficient binary search over the `alpha` parameter space to perfectly select the target K features. It also splits the output datasets sequentially as required and dumps all results into the CSV and JSON files.
```

### 7. Files generated or downloaded from the LLM response

```yaml
generated_or_downloaded_files:
  - unique_file_name: "LLM_G04-06-05-001_feature_selection.py"
    repository_path: "reports/llm_usage/files/LLM_G04-06-05-001_feature_selection.py"
    file_type: "py"
    created_from_response: true
    short_description: "Implementation of Phase 2 logic using Simulated Annealing."
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true
  modified_before_use: false
  description_of_modifications: null
  related_repository_files: 
    - "src/qubo_project/feature_selection.py"
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: "no"
```

### 10. Usefulness and reliability assessment

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: "yes"
  notes: "The code mathematically matches the QUBO definition from the appendix."
```

---

## Interaction G04-06-05-002

### 1. LLM and chat information

```yaml
llm_name: "Gemini"
llm_version_or_model: "Gemini 3.1 Pro"
chat_name_or_identifier: "IDE_session_phase2_debugging"
interaction_mode: "IDE_assistant"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"
```

### 3. Project phase

```yaml
project_phase:
  - "debugging"
```

### 4. Input files and/or context provided to the LLM

```yaml
input_context:
  files_uploaded:
    - file_name: "feature_selection.py"
      repository_path: "src/qubo_project/feature_selection.py"
      description: "Current implementation of the feature selection algorithm."
  code_snippets: "no"
  error_messages: "yes"
  dataset_excerpts: "no"
  prompt_generated_by: null 
```

### 5. Student prompt

```text
While running the `feature_selection.py` script, I encountered the following error during the JSON output generation:
`TypeError: Object of type int8 is not JSON serializable`

The error occurs because the `vector` extracted from the `best_sample` returned by the `neal` Simulated Annealing sampler contains `numpy.int8` values instead of native python `int` values. Furthermore, `np.mean` and `np.std` used for calculating `mean_optimization_time` and `std_dev_optimization_time` return `numpy.float64` objects which can also cause JSON serialization issues.

Please update the `select_features` function in `src/qubo_project/feature_selection.py` to explicitly cast the elements of the `vector` to standard python `int()`, and the numpy mean/std metrics to standard python `float()`, so that the dictionary can be successfully saved using `json.dump()`.
```

### 6. LLM response

```text
I've successfully identified the root cause of the error. Numpy numeric types are indeed not fully compatible with Python's native `json.dump()`. I have updated the code to cast the vector elements to `int()` and the optimization times to `float()`. The json statistics will now be written out cleanly and without throwing TypeErrors.
```

### 7. Files generated or downloaded from the LLM response

```yaml
generated_or_downloaded_files: []
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true
  modified_before_use: false
  description_of_modifications: null
  related_repository_files:
    - "src/qubo_project/feature_selection.py"
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: "yes"
  issue_categories:
    - "runtime_error"
  description: "Type serialization error due to numpy objects in dictionaries intended for JSON output."
  how_issue_was_resolved: "Forced type casting to standard Python int and float types."
```

### 10. Usefulness and reliability assessment

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: "yes"
  notes: "The fix correctly resolved the runtime error in seconds."
```

---

## Interaction G04-06-05-003

### 1. LLM and chat information

```yaml
llm_name: "Gemini"
llm_version_or_model: "Gemini 3.1 Pro"
chat_name_or_identifier: "IDE_session_phase2_testing"
interaction_mode: "IDE_assistant"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"
```

### 3. Project phase

```yaml
project_phase:
  - "testing"
```

### 4. Input files and/or context provided to the LLM

```yaml
input_context:
  files_uploaded:
    - file_name: "README.md"
      repository_path: ".artifacts/README.md"
      description: "Project specification detailing automated testing."
  code_snippets: "no"
  error_messages: "no"
  dataset_excerpts: "no"
  prompt_generated_by: null 
```

### 5. Student prompt

```text
According to section 13 of the project requirements, I need an automated test for Phase 2 that uses pytest. 

Please create the file `tests/test_feature_selection.py`. The test should:
1. Use `pytest.fixture` and `tempfile` to avoid leaving artifacts on the filesystem.
2. Generate a mock normalized dataset with 10 features and 100 samples, and an artificial target correlated to some of the features.
3. Run the `select_features` function with `percSelected=0.20`.
4. Assert that the output `selected_vector` is binary (only contains 0s and 1s).
5. Assert that the number of selected features is approximately 20% of the input features (with a tolerance of 1).
6. Verify that the output files (`reducedTrain.csv`, `reducedTest.csv`, `optimizations.csv`, `stats.json`) are correctly created and that the reduced CSVs have the correct number of columns and the correct train/test split ratio (e.g. 70/30).
```

### 6. LLM response

```text
Here is the requested test file for `pytest`. It programmatically builds a mock dataset to ensure reliable outputs. The test uses standard pytest assertions to ensure that the array is strictly binary, that exactly ~20% of features are maintained, and that the split into training/testing CSV files respects the requested size dimension proportions.
```

### 7. Files generated or downloaded from the LLM response

```yaml
generated_or_downloaded_files:
  - unique_file_name: "LLM_G04-06-05-003_test_feature_selection.py"
    repository_path: "reports/llm_usage/files/LLM_G04-06-05-003_test_feature_selection.py"
    file_type: "py"
    created_from_response: true
    short_description: "Automated test script for pytest verification."
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true
  modified_before_use: false
  description_of_modifications: null
  related_repository_files: 
    - "tests/test_feature_selection.py"
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: "no"
```

### 10. Usefulness and reliability assessment

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: "yes"
  notes: "The generated test covers all conditions from point 13 and runs successfully on the first attempt."
```
