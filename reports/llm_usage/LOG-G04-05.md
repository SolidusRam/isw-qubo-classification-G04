# LLM Interaction Log

---

## Metadata

```yaml
group_id: "G04"
repository_url: "https://github.com/micheledmi/isw-qubo-classification-G04"
students:
  - matricola: "66469"
    name: "Matteo Parasuco"
  - matricola: "66474"
    name: "Igor Derosas"
last_update: "2026-06-06"
log_id: "G04-05"
```

---

# Interaction Entries

---

## Interaction G04-06-06-001

### 1. LLM and chat information

```yaml
llm_name: "Gemini"
llm_version_or_model: "Gemini 3.1 Pro (High)"
chat_name_or_identifier: "Step 3 classification model implementation"
interaction_mode: "IDE_assistant"
```

### 2. Author of the interaction

```yaml
performed_by: "couple"
```

### 3. Project phase

```yaml
project_phase:
  - "classification_model"
  - "testing"
```

### 4. Input files and/or context provided to the LLM

```yaml
input_context:
  files_uploaded:
    - file_name: "specifiche.md"
      repository_path: ".artifacts/specifiche.md"
      description: "Project requirements"
    - file_name: "README.md"
      repository_path: "README.md"
      description: "Project read me"
    - file_name: "requirements.txt"
      repository_path: "requirements.txt"
      description: "Dependencies"
  code_snippets: "no"
  error_messages: "no"
  dataset_excerpts: "no"
  prompt_generated_by: null 
```

### 5. Student prompt

```text
id like to discuss the implementation of the 3 step og the project, lets discuss the specification, consider that we need to select the algoritm, we should also take note of the extra points for having a good score on the F value. All the requirements are written in the .artifacts folder

its good,lets write why we choose this algorims into the readme and start the implementation.

lets test the results
```

### 6. LLM response

```text
The LLM explained the requirements for Step 3 (learning del classificatore), and suggested implementing Random Forest (mandatory), LightGBM (for excellent performance on tabular data and dealing with the 1.5 million row verification dataset), and Logistic Regression (for a fast baseline).
The LLM successfully added lightgbm to requirements.txt, appended the algorithm choice explanation to README.md, and created the complete src/qubo_project/model.py file featuring train() and predict() methods with class balancing to optimize the F1 score for the positive class.
Finally, the LLM ran testing on outputs/test_reduced.csv using the trained LightGBM model, achieving an F1 score of 0.64 for the minority class, which was evaluated as solid.
```

### 7. Files generated or downloaded from the LLM response

```yaml
generated_or_downloaded_files:
  - unique_file_name: "LLM_G04-06-06-001_model.py"
    repository_path: "reports/llm_usage/files/LLM_G04-06-06-001_model.py"
    file_type: "py"
    created_from_response: true
    short_description: "Implementation of the classification models"
    referenced_in_response: true
```

### 8. How the LLM output was used

```yaml
usage_of_output:
  used_without_changes: true
  modified_before_use: false
  description_of_modifications: "N/A"
  related_repository_files:
    - "src/qubo_project/model.py"
    - "README.md"
    - "requirements.txt"
```

### 9. Problems, errors, or hallucinations

```yaml
issues_found:
  any_issue_found: "no"
```

### 10. Usefulness and reliability assessment

```yaml
assessment:
  usefulness_1_to_5: 5
  correctness_1_to_5: 5
  clarity_1_to_5: 5
  confidence_after_verification_1_to_5: 5
  would_reuse_this_output: "yes"
  notes: "The LLM properly understood all requirements, successfully picked appropriate algorithms targeting the bonus points, implemented correctly without errors, and successfully tested it automatically via command line."
```
